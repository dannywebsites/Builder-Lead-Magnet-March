---
phase: 08-gdpr-compliance-privacy-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacy/page.tsx
  - src/components/ui/ConsentCheckbox.tsx
  - src/components/ui/ConsentCheckbox.test.tsx
  - src/app/layout.tsx
  - src/components/ui/Footer.tsx
autonomous: true
requirements: [LEAD-02, UX-03]

must_haves:
  truths:
    - "Privacy policy page is accessible at /privacy with plain-English content"
    - "GDPR consent checkbox component exists, is unchecked by default, and exposes checked state"
    - "Privacy policy link is visible in a footer on every page"
  artifacts:
    - path: "src/app/privacy/page.tsx"
      provides: "Privacy policy page"
      contains: "Privacy Policy"
    - path: "src/components/ui/ConsentCheckbox.tsx"
      provides: "Reusable GDPR consent checkbox for Phase 9 email modal"
      exports: ["ConsentCheckbox"]
    - path: "src/components/ui/Footer.tsx"
      provides: "Site footer with privacy policy link"
      exports: ["Footer"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/ui/Footer.tsx"
      via: "import and render in RootLayout"
      pattern: "import.*Footer.*from"
    - from: "src/components/ui/Footer.tsx"
      to: "/privacy"
      via: "Next.js Link component"
      pattern: "href.*privacy"
---

<objective>
Create the GDPR compliance infrastructure: a privacy policy page, a reusable consent checkbox component, and a site footer with privacy link. This lays the legal foundation required before Phase 9 captures any email addresses.

Purpose: GDPR requires informed consent before collecting personal data. This phase builds the consent UI component and privacy policy so Phase 9 can wire them into the email capture modal.
Output: Privacy policy page at /privacy, ConsentCheckbox component, Footer component with privacy link in root layout.

Note on UX-03 (cookieless analytics): Per user decision D-06/D-07, analytics integration is deferred to a future phase. UX-03 is covered by this plan in the sense that no cookie-based analytics are introduced, preserving the cookieless requirement. The active analytics tool selection will happen later.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gdpr-compliance-privacy-infrastructure/08-CONTEXT.md

@src/app/layout.tsx
@src/components/ui/FormField.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Privacy policy page and Footer component</name>
  <files>src/app/privacy/page.tsx, src/components/ui/Footer.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current root layout to extend with Footer)
    - src/app/page.tsx (existing page pattern)
    - .planning/phases/08-gdpr-compliance-privacy-infrastructure/08-CONTEXT.md (D-01, D-02 decisions)
  </read_first>
  <action>
1. Create `src/app/privacy/page.tsx` — a Next.js App Router page (server component, no "use client") per D-02. Include:
   - Exported `metadata` object: `{ title: "Privacy Policy | Trade Survival Calculator", description: "How we handle your data" }`
   - Default export `PrivacyPage` component rendering the privacy policy content
   - Page structure: `<main className="max-w-2xl mx-auto px-4 py-12">` containing:
     - `<h1 className="text-3xl font-bold mb-8">Privacy Policy</h1>`
     - Sections as `<h2>` with `<p>` body text. Plain English per D-01. Sections:
       - **What we collect**: "When you request your Trade Survival Report, we ask for your email address. That is the only personal data we collect."
       - **Why we collect it**: "We use your email address to: (1) Send you your Trade Survival Report, (2) Occasionally send you practical tips for running a profitable trade business. You can unsubscribe from tips at any time using the link in every email."
       - **What we do NOT do**: "We do not use cookies or tracking scripts that require consent. We do not share, sell, or give your email address to anyone. We do not store any of the financial figures you enter into the calculator — all calculations happen in your browser and are never sent to our servers."
       - **Your rights**: "You can ask us to delete your email address at any time by emailing [contact email placeholder]. We will remove it within 30 days. Under GDPR and the UK Data Protection Act 2018, you also have the right to access, correct, or restrict processing of your data."
       - **Who we are**: "Trade Survival Calculator is operated by [Business Name placeholder]. For any privacy questions, email [contact email placeholder]."
     - At the bottom: `<p className="text-sm text-gray-500 mt-8">Last updated: March 2026</p>`
   - Include a "Back to calculator" link at the top: `<Link href="/" className="text-blue-600 hover:underline text-sm">&larr; Back to calculator</Link>`

2. Create `src/components/ui/Footer.tsx` — a client-free server component:
   - Export named `Footer` component
   - Render: `<footer className="mt-auto border-t border-gray-200 py-6 px-4 text-center text-sm text-gray-500">`
   - Inside footer: `<Link href="/privacy" className="hover:underline">Privacy Policy</Link>`
   - Add a separator and copyright: `<span className="mx-2">·</span><span>&copy; {new Date().getFullYear()} Trade Survival Calculator</span>`

3. Update `src/app/layout.tsx`:
   - Add import: `import { Footer } from "@/components/ui/Footer";`
   - Place `<Footer />` after the NuqsAdapter children wrapper, still inside `<body>` but after the main content. The body already has `flex flex-col` and `min-h-full`, so footer will stick to bottom.
   - Resulting body structure: `<body className="min-h-full flex flex-col"><NuqsAdapter>{children}</NuqsAdapter><Footer /></body>`
  </action>
  <verify>
    <automated>cd /Users/danny/Desktop/Lead\ Magnus\ Build\ out && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/privacy/page.tsx exists and contains "Privacy Policy"
    - src/app/privacy/page.tsx contains "What we collect" and "Your rights"
    - src/app/privacy/page.tsx contains export const metadata with title "Privacy Policy"
    - src/app/privacy/page.tsx contains Link href="/" for back navigation
    - src/components/ui/Footer.tsx exists and contains export function Footer
    - src/components/ui/Footer.tsx contains href="/privacy"
    - src/app/layout.tsx contains import { Footer } from "@/components/ui/Footer"
    - src/app/layout.tsx contains <Footer />
    - next build completes without errors
  </acceptance_criteria>
  <done>Privacy policy page renders at /privacy with plain-English GDPR content. Footer with privacy link appears on every page via root layout. Build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reusable GDPR consent checkbox component</name>
  <files>src/components/ui/ConsentCheckbox.tsx, src/components/ui/ConsentCheckbox.test.tsx</files>
  <read_first>
    - src/components/ui/FormField.tsx (existing UI component pattern)
    - src/components/ui/Footer.tsx (just created, for pattern consistency)
    - src/app/privacy/page.tsx (just created, to confirm /privacy route exists)
    - .planning/phases/08-gdpr-compliance-privacy-infrastructure/08-CONTEXT.md (D-03, D-04, D-05 decisions)
  </read_first>
  <behavior>
    - Test 1: Renders with checkbox input that is unchecked by default (per D-05)
    - Test 2: Renders consent label text containing "I agree to receive my report and occasional tips by email" (per D-04)
    - Test 3: Renders a link to /privacy within the label text containing "Privacy Policy"
    - Test 4: Calls onChange callback when checkbox is toggled
    - Test 5: Displays error message when error prop is provided
    - Test 6: Checkbox has id="consent" and name="consent" for form integration
  </behavior>
  <action>
1. Create `src/components/ui/ConsentCheckbox.test.tsx`:
   - Import render, screen, fireEvent from @testing-library/react
   - Import ConsentCheckbox from ./ConsentCheckbox
   - Test suite "ConsentCheckbox" with tests matching the behavior list above
   - For Test 1: `expect(screen.getByRole('checkbox')).not.toBeChecked()`
   - For Test 2: `expect(screen.getByText(/I agree to receive my report/i)).toBeInTheDocument()`
   - For Test 3: `expect(screen.getByRole('link', { name: /privacy policy/i })).toHaveAttribute('href', '/privacy')`
   - For Test 4: Render with `onChange={mockFn}`, click checkbox, `expect(mockFn).toHaveBeenCalled()`
   - For Test 5: Render with `error="You must agree to continue"`, `expect(screen.getByText('You must agree to continue')).toBeInTheDocument()`, verify `role="alert"` is present
   - For Test 6: `expect(screen.getByRole('checkbox')).toHaveAttribute('id', 'consent')`

2. Create `src/components/ui/ConsentCheckbox.tsx`:
   - "use client" directive (has onChange handler)
   - Props interface `ConsentCheckboxProps`:
     - `checked: boolean`
     - `onChange: (checked: boolean) => void`
     - `error?: string`
   - Export named `ConsentCheckbox` component
   - Render structure:
     ```
     <div className="space-y-1">
       <label className="flex items-start gap-3 cursor-pointer">
         <input
           type="checkbox"
           id="consent"
           name="consent"
           checked={checked}
           onChange={(e) => onChange(e.target.checked)}
           className="mt-1 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
         />
         <span className="text-sm text-gray-700">
           I agree to receive my report and occasional tips by email. See our{' '}
           <Link href="/privacy" className="text-blue-600 hover:underline" target="_blank">Privacy Policy</Link>.
         </span>
       </label>
       {error && <p className="text-sm text-red-600 ml-7" role="alert">{error}</p>}
     </div>
     ```
   - Import Link from "next/link"
   - Component is controlled (checked/onChange) so Phase 9 can integrate it with react-hook-form or standalone state

3. Run tests — all 6 must pass.
  </action>
  <verify>
    <automated>cd /Users/danny/Desktop/Lead\ Magnus\ Build\ out && npx vitest run src/components/ui/ConsentCheckbox.test.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ConsentCheckbox.tsx exists and contains "use client"
    - src/components/ui/ConsentCheckbox.tsx contains export function ConsentCheckbox or export const ConsentCheckbox
    - src/components/ui/ConsentCheckbox.tsx contains id="consent" and name="consent"
    - src/components/ui/ConsentCheckbox.tsx contains "I agree to receive my report"
    - src/components/ui/ConsentCheckbox.tsx contains href="/privacy"
    - src/components/ui/ConsentCheckbox.tsx contains role="alert" in error display
    - src/components/ui/ConsentCheckbox.test.tsx exists and contains at least 6 test cases
    - All tests in ConsentCheckbox.test.tsx pass (exit code 0)
  </acceptance_criteria>
  <done>ConsentCheckbox component is tested, unchecked by default, shows consent text per D-04, links to /privacy, exposes controlled checked/onChange API for Phase 9 integration, and displays validation errors.</done>
</task>

</tasks>

<verification>
1. `next build` completes without errors (static export works)
2. All ConsentCheckbox tests pass
3. /privacy route exists with privacy policy content
4. Footer with privacy link renders in root layout
5. ConsentCheckbox is unchecked by default and blocks submission when unchecked (tested)
</verification>

<success_criteria>
- Privacy policy page accessible at /privacy with plain-English GDPR content (per D-01, D-02)
- ConsentCheckbox component exists with controlled API, unchecked default, error display (per D-03, D-04, D-05) — ready for Phase 9 integration
- Footer with privacy policy link visible on every page via root layout
- No cookie-based analytics introduced (UX-03 maintained; active analytics deferred per D-06)
- Build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-gdpr-compliance-privacy-infrastructure/08-01-SUMMARY.md`
</output>
