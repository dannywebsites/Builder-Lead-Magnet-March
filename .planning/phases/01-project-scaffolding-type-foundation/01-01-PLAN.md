---
phase: 01-project-scaffolding-type-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - biome.json
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - FORM-01

must_haves:
  truths:
    - "Next.js 15 app builds without errors via `pnpm build`"
    - "Dev server starts and serves a page at localhost:3000"
    - "TypeScript strict mode is enabled"
    - "Biome lints and formats without errors"
    - "Vitest runs (even with zero tests) without config errors"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Next.js, React 19, TypeScript, Tailwind 4, Zod, Vitest"
      contains: "next"
    - path: "biome.json"
      provides: "Linting and formatting configuration"
      contains: "formatter"
    - path: "vitest.config.ts"
      provides: "Test runner configuration with path aliases"
      contains: "vitest"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict mode"
      contains: "strict"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS entry point"
      contains: "@import"
  key_links:
    - from: "postcss.config.mjs"
      to: "tailwindcss"
      via: "@tailwindcss/postcss plugin"
      pattern: "@tailwindcss/postcss"
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "path alias resolution"
      pattern: "@/\\*"
---

<objective>
Scaffold a greenfield Next.js 15 project with TypeScript, Tailwind CSS 4, Biome, and Vitest configured. This is the foundation every subsequent phase builds on.

Purpose: Establish the project skeleton, toolchain, and test infrastructure so Plan 02 can immediately define types, schemas, and utilities without any setup friction.
Output: A buildable, lintable, testable Next.js 15 project in the repository root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-type-foundation/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-type-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and install dependencies</name>
  <files>
    package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, postcss.config.mjs,
    biome.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css
  </files>
  <read_first>
    .planning/phases/01-project-scaffolding-type-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Run the Next.js scaffolding command from the project root. The project files should be created IN the current directory (not a subdirectory). Use `--use-pnpm`:

    ```bash
    pnpm create next-app@latest . \
      --typescript \
      --tailwind \
      --app \
      --src-dir \
      --import-alias "@/*" \
      --use-pnpm
    ```

    IMPORTANT: The CLI will prompt for linter choice. Select **Biome** (not ESLint). If the CLI does not offer Biome, proceed with the default and configure Biome manually afterward.

    After scaffolding completes:

    1. **Verify no ESLint remnants**: Check for `.eslintrc*` files or `eslint`/`eslint-config-next` in package.json. Remove any ESLint-related config and packages if present:
       ```bash
       pnpm remove eslint eslint-config-next 2>/dev/null
       rm -f .eslintrc* 2>/dev/null
       ```

    2. **Install Zod** (only additional runtime dependency for this phase):
       ```bash
       pnpm add zod
       ```

    3. **Install Vitest and test utilities as dev dependencies**:
       ```bash
       pnpm add -D vitest @vitejs/plugin-react
       ```

    4. **Verify globals.css uses Tailwind v4 syntax**. The file MUST contain `@import "tailwindcss";` and must NOT contain the v3 directives `@tailwind base`, `@tailwind components`, or `@tailwind utilities`. If it has v3 syntax, replace the entire file content with:
       ```css
       @import "tailwindcss";
       ```

    5. **Verify postcss.config.mjs uses `@tailwindcss/postcss`** (not `tailwindcss` directly). Expected content:
       ```javascript
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```

    6. **Verify NO `tailwind.config.js` or `tailwind.config.ts` exists**. Delete if present (Tailwind v4 does not use JS config).

    7. **Verify biome.json exists**. If `create-next-app` did not create it, create it with this content:
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
         "vcs": {
           "enabled": true,
           "clientKind": "git",
           "useIgnoreFile": true
         },
         "formatter": {
           "enabled": true,
           "indentStyle": "tab",
           "lineWidth": 100
         },
         "linter": {
           "enabled": true,
           "rules": {
             "recommended": true
           }
         },
         "javascript": {
           "formatter": {
             "quoteStyle": "double",
             "semicolons": "always"
           }
         }
       }
       ```
       Also ensure `@biomejs/biome` is in devDependencies. If not: `pnpm add -D @biomejs/biome`

    8. **Verify tsconfig.json has strict mode enabled**. The `"strict": true` flag must be present under `compilerOptions`. If missing, add it.

    9. **Simplify src/app/page.tsx** to a minimal placeholder (remove the default Next.js boilerplate):
       ```tsx
       export default function Home() {
         return (
           <main>
             <h1>Trade Survival Calculator</h1>
             <p>Coming soon.</p>
           </main>
         );
       }
       ```

    10. **Create the calculator module directory structure** (empty for now, Plan 02 fills it):
        ```bash
        mkdir -p src/lib/calculator/__tests__
        ```
  </action>
  <verify>
    <automated>cd "/Users/danny/Desktop/Lead Magnus Build out" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies
    - package.json contains "zod" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "@biomejs/biome" in devDependencies
    - tsconfig.json contains "strict": true (or "strict" is true)
    - src/app/globals.css contains `@import "tailwindcss"`
    - src/app/globals.css does NOT contain `@tailwind base`
    - postcss.config.mjs contains "@tailwindcss/postcss"
    - No file named tailwind.config.js or tailwind.config.ts exists
    - No file named .eslintrc or .eslintrc.json exists
    - biome.json exists and contains "formatter"
    - src/app/page.tsx contains "Trade Survival Calculator"
    - Directory src/lib/calculator/__tests__ exists
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 15 project builds successfully with TypeScript strict mode, Tailwind v4, Biome, Zod, and Vitest installed. No ESLint remnants. Calculator module directory structure exists.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest with path aliases and verify toolchain</name>
  <files>vitest.config.ts, package.json</files>
  <read_first>
    tsconfig.json
    package.json
  </read_first>
  <action>
    1. **Create `vitest.config.ts`** in the project root with path alias support matching tsconfig:
       ```typescript
       import react from "@vitejs/plugin-react";
       import { resolve } from "path";
       import { defineConfig } from "vitest/config";

       export default defineConfig({
         plugins: [react()],
         resolve: {
           alias: {
             "@": resolve(__dirname, "./src"),
           },
         },
         test: {
           globals: true,
           environment: "node",
           include: ["src/**/*.test.ts", "src/**/*.test.tsx"],
         },
       });
       ```

    2. **Add test script to package.json** under `"scripts"`:
       ```json
       "test": "vitest run --reporter=verbose",
       "test:watch": "vitest --reporter=verbose"
       ```

    3. **Add format and lint scripts to package.json** under `"scripts"`:
       ```json
       "lint": "biome check .",
       "format": "biome format . --write"
       ```

    4. **Initialize git repository** if not already initialized:
       ```bash
       git init
       ```

    5. **Create .gitignore** if not present or verify it includes:
       ```
       node_modules/
       .next/
       out/
       .vercel/
       *.tsbuildinfo
       next-env.d.ts
       ```

    6. **Run the full toolchain verification**:
       - `pnpm build` must exit 0
       - `pnpm vitest run` must exit 0 (no tests yet, but config is valid)
       - `pnpm biome check .` must exit 0 (or only warnings, no errors)
  </action>
  <verify>
    <automated>cd "/Users/danny/Desktop/Lead Magnus Build out" && pnpm vitest run 2>&1 | tail -5 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists and contains `"@": resolve(__dirname, "./src")`
    - vitest.config.ts contains `environment: "node"`
    - package.json scripts contains "test": "vitest run --reporter=verbose"
    - package.json scripts contains "lint": "biome check ."
    - `pnpm vitest run` exits with code 0
    - `pnpm build` exits with code 0
    - .gitignore contains "node_modules/"
    - .gitignore contains ".next/"
  </acceptance_criteria>
  <done>Vitest configured with path aliases matching tsconfig. All toolchain commands (build, test, lint) run without errors. Git initialized with proper .gitignore.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `pnpm vitest run` completes without config errors (0 tests is OK)
3. `pnpm biome check .` reports no errors
4. No ESLint config files exist
5. No Tailwind JS config files exist
6. `src/lib/calculator/__tests__/` directory exists
</verification>

<success_criteria>
A clean, buildable Next.js 15 project with TypeScript strict mode, Tailwind CSS 4 (CSS-native config), Biome (no ESLint), Zod installed, and Vitest configured with path aliases. The calculator module directory structure is ready for Plan 02 to populate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-type-foundation/01-01-SUMMARY.md`
</output>
