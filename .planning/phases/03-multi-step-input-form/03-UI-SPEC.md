---
phase: 3
slug: multi-step-input-form
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-28
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the multi-step input form wizard. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom Tailwind components) |
| Icon library | none (no icons needed this phase) |
| Font | Geist Sans (variable: `--font-geist-sans`, loaded via `next/font/google`) |

**Rationale:** RESEARCH.md and CLAUDE.md explicitly specify custom Tailwind components with no UI library. Geist Sans is already configured in `layout.tsx`. No icons are needed for form inputs and navigation buttons in this phase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline spacing between label and required indicator |
| sm | 8px | Gap between input and error message, padding inside compact elements |
| md | 16px | Default padding inside form fields, gap between form fields |
| lg | 24px | Padding inside step cards, gap between field groups |
| xl | 32px | Vertical gap between step content and navigation buttons |
| 2xl | 48px | Top/bottom padding of the wizard container |
| 3xl | 64px | Not used this phase |

Exceptions: Touch target minimum height is 44px for all interactive elements (inputs, buttons, selects) per WCAG 2.5.5.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (regular) | 1.5 | Form field helper text, step descriptions |
| Label | 14px | 600 (semibold) | 1.4 | Form field labels, progress step labels |
| Heading | 24px | 600 (semibold) | 1.2 | Step titles (e.g., "Your Business", "Your Money") |
| Error | 14px | 400 (regular) | 1.4 | Validation error messages |

**Font stack:** `var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif`

**Notes:**
- Only 3 sizes declared (14px, 16px, 24px) to maintain visual discipline across the 4-step form.
- Only 2 weights used: 400 (regular) for body text, helper text, and error messages; 600 (semibold) for labels and headings.
- Labels share the semibold weight with headings but are differentiated by size (14px vs 24px), providing clear hierarchy without a third weight.
- All text renders in Geist Sans via the CSS variable already configured in `layout.tsx`.

---

## Color

| Role | Value | Tailwind Class | Usage |
|------|-------|----------------|-------|
| Dominant (60%) | `#ffffff` | `bg-white` | Page background, form field backgrounds |
| Secondary (30%) | `#f9fafb` | `bg-gray-50` | Step card backgrounds, disabled field backgrounds |
| Accent (10%) | `#2563eb` | `bg-blue-600 text-blue-600` | Primary CTA button, focused input ring, active step indicator |
| Destructive | `#dc2626` | `text-red-600 border-red-500` | Validation error text, error border on invalid fields |
| Border default | `#d1d5db` | `border-gray-300` | Input borders in resting state |
| Text primary | `#171717` | `text-foreground` | All body text, labels, headings |
| Text secondary | `#6b7280` | `text-gray-500` | Helper text, currency prefix/suffix, disabled text |

**Accent reserved for:**
- "Next Step" / "Calculate" primary CTA buttons (solid background)
- Active step indicator in the progress stepper
- Focused input border ring (`focus:ring-blue-500`)

**Accent is NOT used for:** back buttons, field labels, step card borders, or decorative elements.

**Dark mode:** Not implemented this phase. The existing `prefers-color-scheme: dark` block in `globals.css` is inherited but not designed for. Phase 10 (UX Polish) addresses this if needed.

---

## Component Inventory

Components created in this phase, referenced by the planner and executor:

### Layout Components

| Component | File | Purpose |
|-----------|------|---------|
| `WizardForm` | `src/components/wizard/WizardForm.tsx` | Main wizard container. Client component. Owns `useForm` instance, `useQueryState` for step, step navigation logic. Renders progress indicator + current step + navigation buttons. |
| `WizardProgress` | `src/components/wizard/WizardProgress.tsx` | Horizontal numbered stepper showing 4 steps. Highlights completed (checkmark or filled), current (accent ring), and upcoming (gray) states. |

### Step Components

| Component | File | Fields |
|-----------|------|--------|
| `StepBusinessIdentity` | `src/components/wizard/steps/StepBusinessIdentity.tsx` | Entity Type (select), Currency (select), VAT Rate (select) |
| `StepFinancials` | `src/components/wizard/steps/StepFinancials.tsx` | Gross Personal Draw (number + currency prefix), Fixed Overheads (number + currency prefix) |
| `StepStaff` | `src/components/wizard/steps/StepStaff.tsx` | Staff Count (integer), Staff Hourly Rate (number + currency prefix, disabled when count=0), Staff Hours Per Week (number + "hrs" suffix, disabled when count=0) |
| `StepJobPricing` | `src/components/wizard/steps/StepJobPricing.tsx` | Average Job Value (number + currency prefix), Direct Cost % (number + "%" suffix, range 0-80) |

### Reusable UI Components

| Component | File | Purpose |
|-----------|------|---------|
| `FormField` | `src/components/ui/FormField.tsx` | Wrapper: label + input slot + error message. Handles `aria-describedby` and `aria-invalid` wiring. |
| `NumberInput` | `src/components/ui/NumberInput.tsx` | Number input with optional prefix (currency symbol) and suffix (%, hrs). Uses `valueAsNumber: true` with react-hook-form `register`. |
| `SelectInput` | `src/components/ui/SelectInput.tsx` | Styled `<select>` dropdown for Entity Type, Currency, VAT Rate. |

---

## Interaction Contract

### Progress Indicator (WizardProgress)

| State | Visual Treatment |
|-------|-----------------|
| Completed step | Filled circle with white checkmark on `bg-blue-600`, label in `text-foreground` |
| Current step | Circle with `ring-2 ring-blue-600` outline, step number inside, label in `text-foreground font-semibold` |
| Upcoming step | Circle with `bg-gray-200`, step number in `text-gray-400`, label in `text-gray-400` |
| Connector line (completed) | `bg-blue-600` solid line between circles |
| Connector line (upcoming) | `bg-gray-200` solid line between circles |

Step labels (displayed below each circle):
1. "Business"
2. "Money"
3. "Team"
4. "Pricing"

### Navigation Buttons

| Button | Position | Variant | Visible When |
|--------|----------|---------|-------------|
| "Back" | Bottom-left of step card | Ghost/text style: `text-gray-600 hover:text-foreground` | Steps 2, 3, 4 (hidden on Step 1) |
| "Next Step" | Bottom-right of step card | Primary solid: `bg-blue-600 text-white hover:bg-blue-700` | Steps 1, 2, 3 |
| "Calculate Your Numbers" | Bottom-right of step card | Primary solid: `bg-blue-600 text-white hover:bg-blue-700` | Step 4 only (final step) |

**Button sizing:** `px-6 py-3 rounded-lg text-base font-semibold` -- minimum 44px touch target height.

### Step Navigation Rules

- **Forward:** Clicking "Next Step" triggers validation on the current step's fields only. If all pass, advance to next step. If any fail, show inline errors and do not advance.
- **Backward:** Clicking "Back" navigates to the previous step immediately. No validation on back navigation. All entered data is preserved (single `useForm` instance).
- **Stepper click:** Clicking a completed step circle navigates back to that step. Clicking an upcoming step does nothing. This allows review of completed steps without forcing re-validation.
- **URL state:** Current step stored in URL as `?step=0` through `?step=3` via nuqs. Browser back/forward buttons navigate between steps. Default is `step=0`.

### Validation UX

| Trigger | Behavior |
|---------|----------|
| "Next Step" button click | Validate all fields in the current step. Show errors inline below each invalid field. Focus moves to the first invalid field. |
| Field blur (after first "Next Step" attempt) | Re-validate the blurred field immediately. Clear error if now valid. Mode: `onTouched`. |
| Field change (after error shown) | Clear the specific field error as soon as the user begins correcting it. |

**Error display:** Red text (`text-red-600`, 14px) directly below the invalid field, with 8px gap. The field border changes to `border-red-500`. The error message is linked via `aria-describedby`.

### Zero-Staff Behavior (D-04)

When `staffCount` equals 0:
- `staffHourlyRate` and `staffHoursPerWeek` fields are `disabled`
- Values are set to `0`
- Visual treatment: `opacity-50 cursor-not-allowed bg-gray-100`
- Fields remain visible so the user understands what they would fill in if they had staff
- When `staffCount` changes to 1+, fields become enabled and editable immediately

### Direct Cost % Display (D-03 Resolution)

- **Input method:** Number input (not slider). Tradespeople know their material cost percentage as a number.
- **Display range:** 0 to 80 (user enters whole percentages, e.g., "35" for 35%)
- **Storage:** Transform to decimal (divide by 100) on form submission before passing to `calculate()`
- **HTML attributes:** `min=0 max=80 step="0.1"` with `%` suffix
- **Validation message for >80:** "Direct costs cannot exceed 80% -- this protects your margin"

### Form Submission (Step 4 "Calculate Your Numbers")

1. Validate Step 4 fields
2. If valid, transform form data: convert `directCostPctDisplay` (0-80) to `directCostPct` (0.0-0.8), convert VatRate string to numeric `VatRateValue`
3. Call `calculate()` with transformed `CalculatorInput`
4. Store result in component state (results display is Phase 5 scope)
5. For now: log result to console or store in a ref for Phase 5 to consume

---

## Copywriting Contract

### Step Titles and Descriptions

| Step | Title | Description |
|------|-------|-------------|
| 1 | "Your Business" | "Tell us about your business setup" |
| 2 | "Your Money" | "What you need to take home and what you spend" |
| 3 | "Your Team" | "Staff costs -- skip if you work solo" |
| 4 | "Your Jobs" | "How much your typical job is worth" |

### Field Labels and Placeholders

| Field | Label | Placeholder | Prefix/Suffix |
|-------|-------|-------------|---------------|
| Entity Type | "Business Type" | -- (select, no placeholder) | -- |
| Currency | "Currency" | -- (select, no placeholder) | -- |
| VAT Rate | "VAT Rate" | -- (select, no placeholder) | -- |
| Gross Personal Draw | "Monthly Take-Home Target" | "e.g. 4000" | Currency symbol (dynamic) |
| Fixed Overheads | "Monthly Fixed Costs" | "e.g. 2000" | Currency symbol (dynamic) |
| Staff Count | "Number of Staff" | "0" | -- |
| Staff Hourly Rate | "Staff Hourly Rate" | "e.g. 15" | Currency symbol (dynamic) |
| Staff Hours Per Week | "Staff Hours Per Week" | "e.g. 40" | "hrs" |
| Average Job Value | "Average Job Value (exc. VAT)" | "e.g. 3000" | Currency symbol (dynamic) |
| Direct Cost % | "Direct Costs %" | "e.g. 35" | "%" |

**Currency symbol:** Display `\u00a3` when currency is GBP, `\u20ac` when currency is EUR. Read from the form's `currency` field value via `useWatch`.

### Select Options

| Field | Options |
|-------|---------|
| Entity Type | "Limited Company", "Sole Trader" |
| Currency | "GBP", "EUR" |
| VAT Rate | "20% (UK Standard)", "13.5% (Ireland Reduced)", "23% (Ireland Standard)", "0% (VAT Exempt)" |

### Validation Error Messages

| Field | Rule | Error Message |
|-------|------|---------------|
| Gross Personal Draw | positive | "Enter your monthly take-home target" |
| Fixed Overheads | nonnegative | "Fixed costs cannot be negative" |
| Staff Count | nonnegative integer | "Enter a whole number (0 if you work solo)" |
| Staff Hourly Rate | nonnegative | "Rate cannot be negative" |
| Staff Hours Per Week | nonnegative, max 168 | "Hours cannot be negative" / "Maximum 168 hours per week" |
| Average Job Value | positive | "Enter your average job value" |
| Direct Cost % | min 0, max 80 | "Cannot be negative" / "Direct costs cannot exceed 80% -- this protects your margin" |
| Entity Type | required | "Select your business type" |
| Currency | required | "Select your currency" |
| VAT Rate | required | "Select your VAT rate" |

### Empty State

Not applicable for this phase. The wizard always starts at Step 1 with default/empty fields. There is no "no data" state -- the user is always presented with the first step.

### Error State (Global)

Not applicable for this phase. Errors are field-level inline validation only. No global error banners or API error states exist in a client-side-only form.

### Destructive Actions

None in this phase. There is no delete, reset, or destructive action. The "Back" button preserves data. Navigating away from the page loses form state, which is acceptable behavior for a calculator (no persistence needed per project constraints).

### Primary CTA

- Steps 1-3: **"Next Step"**
- Step 4: **"Calculate Your Numbers"**

---

## Layout Contract

### Focal Point

The visual anchors for each step screen are the step title (24px semibold, top of the step card) and the primary CTA button ("Next Step" or "Calculate Your Numbers", accent-colored, bottom-right of the step card). The user's eye should travel from the title down through the form fields to the CTA -- a single vertical reading line with the two highest-contrast elements bookending the content.

### Page Layout

```
[Page: max-w-2xl mx-auto px-4 py-12]
  [WizardProgress: horizontal stepper, full width]
  [Step Card: bg-white rounded-xl shadow-sm border border-gray-200 p-6 mt-8]
    [Step Title: heading, 24px semibold]
    [Step Description: body, 16px regular, text-gray-500, mt-1]
    [Fields: vertical stack, gap-4, mt-6]
    [Navigation: flex justify-between, mt-8]
```

### Responsive Behavior

- **Mobile (< 640px):** Full-width card with `px-4`. Progress stepper labels hidden; show step numbers only. Fields stack vertically. Buttons stack vertically (Calculate/Next Step on top, Back below) if needed, but prefer side-by-side at this width.
- **Tablet and up (>= 640px):** `max-w-2xl` centered container. Full progress stepper with labels. Buttons side-by-side.
- **No horizontal scrolling** at any viewport width.

### Step Card Dimensions

- Min height: none (content determines height)
- Max width: `max-w-2xl` (672px)
- Padding: 24px (`p-6`)
- Border radius: 12px (`rounded-xl`)
- Border: 1px `border-gray-200`
- Shadow: `shadow-sm`
- Background: `bg-white`

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|---------------|
| Form labels | Every input has a visible `<label>` with matching `htmlFor`/`id` |
| Error association | Invalid fields use `aria-invalid="true"` and `aria-describedby` pointing to error `<p>` |
| Error announcement | Error messages use `role="alert"` so screen readers announce them |
| Focus management | On validation failure, focus moves to the first invalid field |
| Keyboard navigation | Tab order follows visual order. Enter submits current step (Next Step/Calculate). |
| Disabled fields | Disabled inputs use native `disabled` attribute (removed from tab order automatically) |
| Touch targets | All interactive elements are at least 44px tall |
| Color contrast | Text on white: `#171717` = 15.4:1 ratio (AAA). Error red on white: `#dc2626` = 4.6:1 (AA). Gray-500 on white: `#6b7280` = 5.0:1 (AA). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No component registries are used. All components are custom-built with Tailwind CSS per the project stack decision.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
