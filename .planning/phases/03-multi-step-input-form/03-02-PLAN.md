---
phase: 03-multi-step-input-form
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/wizard/steps/StepBusinessIdentity.tsx
  - src/components/wizard/steps/StepFinancials.tsx
  - src/components/wizard/steps/StepStaff.tsx
  - src/components/wizard/steps/StepJobPricing.tsx
  - src/components/wizard/WizardForm.tsx
autonomous: false
requirements: [FORM-02, FORM-03, FORM-04, FORM-05, FORM-06, FORM-07, FORM-08]

must_haves:
  truths:
    - "User can enter Gross Personal Draw in Step 2 with currency prefix"
    - "User can enter Fixed Overheads in Step 2 with currency prefix"
    - "User can enter Staff Count in Step 3 as a whole number"
    - "Staff Hourly Rate and Staff Hours Per Week are disabled and greyed out when Staff Count is 0"
    - "User can enter Average Job Value in Step 4 with currency prefix"
    - "User can enter Direct Cost % in Step 4 as a percentage (0-80) with % suffix"
    - "Step 1 has Entity Type, Currency, and VAT Rate as separate dropdowns"
    - "Validation errors show inline below invalid fields when clicking Next Step"
    - "Form data persists when navigating back and forward between steps"
    - "Clicking Calculate Your Numbers on Step 4 calls calculate() with transformed data"
  artifacts:
    - path: "src/components/wizard/steps/StepBusinessIdentity.tsx"
      provides: "Step 1 fields: Entity Type, Currency, VAT Rate dropdowns"
      contains: "use client"
    - path: "src/components/wizard/steps/StepFinancials.tsx"
      provides: "Step 2 fields: Gross Personal Draw, Fixed Overheads"
      contains: "use client"
    - path: "src/components/wizard/steps/StepStaff.tsx"
      provides: "Step 3 fields: Staff Count, Staff Hourly Rate, Staff Hours Per Week"
      contains: "useWatch"
    - path: "src/components/wizard/steps/StepJobPricing.tsx"
      provides: "Step 4 fields: Average Job Value, Direct Cost %"
      contains: "directCostPctDisplay"
  key_links:
    - from: "src/components/wizard/steps/StepBusinessIdentity.tsx"
      to: "src/components/ui/SelectInput.tsx"
      via: "renders 3 SelectInput components"
      pattern: "SelectInput"
    - from: "src/components/wizard/steps/StepFinancials.tsx"
      to: "src/components/ui/NumberInput.tsx"
      via: "renders 2 NumberInput components with currency prefix"
      pattern: "NumberInput"
    - from: "src/components/wizard/steps/StepStaff.tsx"
      to: "src/components/ui/NumberInput.tsx"
      via: "renders 3 NumberInput components with conditional disable"
      pattern: "disabled={isStaffDisabled}"
    - from: "src/components/wizard/WizardForm.tsx"
      to: "src/components/wizard/steps/*"
      via: "conditional rendering based on step index"
      pattern: "step === 0"
---

<objective>
Build the 4 step components with all input fields, wire them into the wizard shell, and verify the complete form flow from Step 1 through calculation.

Purpose: Completes the multi-step input form with all 8 user-facing fields (FORM-02 through FORM-08), per-step validation on navigation, zero-staff disable behavior, and engine wiring.
Output: Fully functional 4-step wizard form that collects all calculator inputs and produces a calculation result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-step-input-form/03-CONTEXT.md
@.planning/phases/03-multi-step-input-form/03-RESEARCH.md
@.planning/phases/03-multi-step-input-form/03-UI-SPEC.md
@.planning/phases/03-multi-step-input-form/03-01-SUMMARY.md

@src/lib/form/step-schemas.ts
@src/lib/form/form-defaults.ts
@src/lib/form/transform.ts
@src/components/ui/NumberInput.tsx
@src/components/ui/SelectInput.tsx
@src/components/ui/FormField.tsx
@src/components/wizard/WizardForm.tsx
@src/components/wizard/WizardProgress.tsx
@src/lib/calculator/types.ts
@src/lib/calculator/schemas.ts

<interfaces>
<!-- Contracts from Plan 01 that this plan implements against. -->

From src/lib/form/step-schemas.ts (created in Plan 01):
```typescript
export const Step1Schema = z.object({
  entityType: EntityTypeSchema,
  currency: CurrencySchema,
  vatRate: VatRateSchema,
});
export const Step2Schema = z.object({
  grossPersonalDraw: z.number().positive("Enter your monthly take-home target"),
  fixedOverheads: z.number().nonnegative("Fixed costs cannot be negative"),
});
export const Step3Schema = z.object({
  staffCount: z.number().int(...).nonnegative(...),
  staffHourlyRate: z.number().nonnegative("Rate cannot be negative"),
  staffHoursPerWeek: z.number().nonnegative(...).max(168, ...),
});
export const Step4Schema = z.object({
  avgJobValue: z.number().positive("Enter your average job value"),
  directCostPctDisplay: z.number().min(0, ...).max(80, "Direct costs cannot exceed 80% -- this protects your margin"),
});
export type FormValues = z.infer<Step1> & z.infer<Step2> & z.infer<Step3> & z.infer<Step4>;
```

From src/lib/calculator/types.ts:
```typescript
export const VAT_RATE_OPTIONS: ReadonlyArray<{ label: string; value: VatRateValue }>;
// Options: "20% (UK Standard)", "13.5% (Ireland Reduced)", "23% (Ireland Standard)", "0% (VAT Exempt)"
```

Step component contract (from UI-SPEC):
- Each step component receives no props -- it uses useFormContext() to access form state
- Each step component uses the reusable NumberInput/SelectInput components
- Currency prefix is dynamic: read `currency` field via useWatch, display pound or euro symbol
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all 4 step components and wire into WizardForm</name>
  <files>
    src/components/wizard/steps/StepBusinessIdentity.tsx
    src/components/wizard/steps/StepFinancials.tsx
    src/components/wizard/steps/StepStaff.tsx
    src/components/wizard/steps/StepJobPricing.tsx
    src/components/wizard/WizardForm.tsx
  </files>
  <read_first>
    src/components/wizard/WizardForm.tsx
    src/components/ui/NumberInput.tsx
    src/components/ui/SelectInput.tsx
    src/components/ui/FormField.tsx
    src/lib/form/step-schemas.ts
    src/lib/form/form-defaults.ts
    src/lib/form/transform.ts
    src/lib/calculator/types.ts
    src/lib/calculator/schemas.ts
    .planning/phases/03-multi-step-input-form/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create `src/components/wizard/steps/StepBusinessIdentity.tsx` ("use client"):
       - Import `SelectInput` from `"@/components/ui/SelectInput"`
       - Render 3 SelectInput components per D-01 and D-02 (separate dropdowns, no auto-coupling):
         a. Entity Type: `name="entityType"`, `label="Business Type"`, options: `[{ label: "Limited Company", value: "limited_company" }, { label: "Sole Trader", value: "sole_trader" }]`
         b. Currency: `name="currency"`, `label="Currency"`, options: `[{ label: "GBP (\u00a3)", value: "GBP" }, { label: "EUR (\u20ac)", value: "EUR" }]` per D-05
         c. VAT Rate: `name="vatRate"`, `label="VAT Rate"`, options: `[{ label: "20% (UK Standard)", value: "0.20" }, { label: "13.5% (Ireland Reduced)", value: "0.135" }, { label: "23% (Ireland Standard)", value: "0.23" }, { label: "0% (VAT Exempt)", value: "0" }]`
       - Step title: `<h2 className="text-2xl font-semibold text-foreground">Your Business</h2>` (24px semibold per UI-SPEC)
       - Step description: `<p className="text-base text-gray-500 mt-1">Tell us about your business setup</p>` (16px regular per UI-SPEC)
       - Fields container: `<div className="flex flex-col gap-4 mt-6">` per UI-SPEC layout

    2. Create `src/components/wizard/steps/StepFinancials.tsx` ("use client"):
       - Import `NumberInput` from `"@/components/ui/NumberInput"`, `useWatch` from `"react-hook-form"`
       - Read currency via `const currency = useWatch({ name: "currency" })` to determine prefix symbol
       - Currency prefix: `const currencySymbol = currency === "EUR" ? "\u20ac" : "\u00a3"`
       - Step title: "Your Money", description: "What you need to take home and what you spend"
       - Render 2 NumberInput components:
         a. `name="grossPersonalDraw"`, `label="Monthly Take-Home Target"`, `prefix={currencySymbol}`, `placeholder="e.g. 4000"`, `min={0}`, `step="0.01"`
         b. `name="fixedOverheads"`, `label="Monthly Fixed Costs"`, `prefix={currencySymbol}`, `placeholder="e.g. 2000"`, `min={0}`, `step="0.01"`

    3. Create `src/components/wizard/steps/StepStaff.tsx` ("use client"):
       - Import `NumberInput` from `"@/components/ui/NumberInput"`, `useWatch`, `useFormContext` from `"react-hook-form"`
       - Read `staffCount` via `const staffCount = useWatch({ name: "staffCount" })`
       - Derive `const isStaffDisabled = staffCount === 0 || Number.isNaN(staffCount)` per D-04
       - When staffCount changes to 0, use `useEffect` to `setValue("staffHourlyRate", 0)` and `setValue("staffHoursPerWeek", 0)` to reset disabled fields per D-04
       - Read currency via `useWatch({ name: "currency" })` for prefix symbol
       - Step title: "Your Team", description: "Staff costs -- skip if you work solo"
       - Render 3 NumberInput components:
         a. `name="staffCount"`, `label="Number of Staff"`, `placeholder="0"`, `min={0}`, `step="1"` (integer)
         b. `name="staffHourlyRate"`, `label="Staff Hourly Rate"`, `prefix={currencySymbol}`, `placeholder="e.g. 15"`, `disabled={isStaffDisabled}`, `min={0}`, `step="0.01"`
         c. `name="staffHoursPerWeek"`, `label="Staff Hours Per Week"`, `suffix="hrs"`, `placeholder="e.g. 40"`, `disabled={isStaffDisabled}`, `min={0}`, `max={168}`, `step="0.5"`

    4. Create `src/components/wizard/steps/StepJobPricing.tsx` ("use client"):
       - Import `NumberInput` from `"@/components/ui/NumberInput"`, `useWatch` from `"react-hook-form"`
       - Read currency for prefix symbol
       - Step title: "Your Jobs", description: "How much your typical job is worth"
       - Render 2 NumberInput components:
         a. `name="avgJobValue"`, `label="Average Job Value (exc. VAT)"`, `prefix={currencySymbol}`, `placeholder="e.g. 3000"`, `min={0}`, `step="0.01"`
         b. `name="directCostPctDisplay"`, `label="Direct Costs %"`, `suffix="%"`, `placeholder="e.g. 35"`, `min={0}`, `max={80}`, `step="0.1"`
       - NOTE: field name is `directCostPctDisplay` (0-80 range), matching Step4Schema

    5. Update `src/components/wizard/WizardForm.tsx`:
       - Import all 4 step components from `"./steps/StepBusinessIdentity"`, `"./steps/StepFinancials"`, `"./steps/StepStaff"`, `"./steps/StepJobPricing"`
       - Replace the placeholder step divs with the actual step components:
         ```tsx
         {step === 0 && <StepBusinessIdentity />}
         {step === 1 && <StepFinancials />}
         {step === 2 && <StepStaff />}
         {step === 3 && <StepJobPricing />}
         ```
       - Keep all existing navigation logic (handleNext, handleBack, onSubmit) unchanged
       - The step card wrapper should contain the step component, with step title/description rendered INSIDE each step component (not in WizardForm)
  </action>
  <verify>
    <automated>cd "/Users/danny/Desktop/Lead Magnus Build out" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/wizard/steps/StepBusinessIdentity.tsx` contains `"use client"`, `SelectInput`, `"entityType"`, `"currency"`, `"vatRate"`, `"Your Business"`
    - `src/components/wizard/steps/StepBusinessIdentity.tsx` contains `"limited_company"` and `"sole_trader"` as option values
    - `src/components/wizard/steps/StepBusinessIdentity.tsx` contains `"0.20"`, `"0.135"`, `"0.23"`, `"0"` as VAT rate option values
    - `src/components/wizard/steps/StepFinancials.tsx` contains `"use client"`, `NumberInput`, `"grossPersonalDraw"`, `"fixedOverheads"`, `useWatch`, `"Your Money"`
    - `src/components/wizard/steps/StepFinancials.tsx` contains `currencySymbol` or equivalent dynamic prefix logic
    - `src/components/wizard/steps/StepStaff.tsx` contains `"use client"`, `"staffCount"`, `"staffHourlyRate"`, `"staffHoursPerWeek"`, `useWatch`, `isStaffDisabled` or equivalent
    - `src/components/wizard/steps/StepStaff.tsx` contains `disabled={` pattern (D-04 zero-staff disable)
    - `src/components/wizard/steps/StepStaff.tsx` contains `"Your Team"`
    - `src/components/wizard/steps/StepJobPricing.tsx` contains `"use client"`, `"avgJobValue"`, `"directCostPctDisplay"`, `"Your Jobs"`
    - `src/components/wizard/steps/StepJobPricing.tsx` contains `suffix="%"` or equivalent percent suffix
    - `src/components/wizard/WizardForm.tsx` contains imports for all 4 step components
    - `src/components/wizard/WizardForm.tsx` contains `StepBusinessIdentity`, `StepFinancials`, `StepStaff`, `StepJobPricing`
    - `pnpm build` exits 0
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>All 4 step components render their fields using reusable UI components, zero-staff disable works, currency prefix is dynamic, and the complete wizard flow navigates through all steps to calculation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete wizard form flow</name>
  <files>none -- human verification only</files>
  <action>
    Human verification checkpoint. The executor should run `pnpm dev` and present the verification steps to the user.

    What was built: Complete 4-step wizard form with all calculator input fields, per-step validation, progress indicator, and engine wiring.

    Verification steps to present:
    1. Open http://localhost:3000
    2. Verify Step 1 ("Your Business"): three separate dropdowns for Business Type, Currency, VAT Rate
    3. Verify progress indicator updates as steps are completed
    4. Verify Step 2 ("Your Money"): currency prefix matches Step 1 selection, validation errors show on empty submit
    5. Verify Step 3 ("Your Team"): staff fields disable when count=0, enable when count>0
    6. Verify Step 4 ("Your Jobs"): Direct Cost % with % suffix, "Calculate Your Numbers" button logs result to console
    7. Verify back navigation preserves entered data
    8. Verify URL shows ?step=N and browser back/forward work
  </action>
  <verify>
    <automated>cd "/Users/danny/Desktop/Lead Magnus Build out" && pnpm build</automated>
  </verify>
  <done>User confirms the wizard form flow works correctly across all 4 steps with validation, navigation, and calculation output in console.</done>
</task>

</tasks>

<verification>
1. `pnpm test` -- all tests pass (step schemas, transform, calculator engine)
2. `pnpm build` -- builds successfully with zero errors
3. Manual: Complete wizard flow from Step 1 through calculation with valid data
4. Manual: Validation prevents advancing with invalid data on each step
5. Manual: Zero-staff disable behavior works correctly on Step 3
6. Manual: Back navigation preserves all entered data
7. Manual: URL reflects current step and browser back/forward work
</verification>

<success_criteria>
- All 8 input fields (FORM-02 through FORM-08) are present and functional across 4 wizard steps
- Per-step validation fires on "Next Step" with user-friendly error messages (FORM-12)
- 4-step wizard with progress indicator is navigable forward and backward (FORM-13)
- Direct Cost % input accepts 0-80 and transforms to 0.0-0.8 for calculation
- Staff rate and hours disable when staff count is 0 (D-04)
- Currency prefix updates dynamically based on currency selection (D-05)
- Form submits to calculate() with correctly transformed data (D-07)
- Build passes, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-step-input-form/03-02-SUMMARY.md`
</output>
